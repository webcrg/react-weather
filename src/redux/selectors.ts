export const currentCity = (state) => state.weatherReducer.currentCity;
export const currentWeather = (state) => state.currentWeather.weather;
export const forecast = (state) => state.forecast.forecast;
export const stats = (state) => state.weatherReducer.stats;
export const activePage = (state) => state.tabsReducer.activePage;
export const favorites = (state) => state.favorites.favorites;
